services:
  node_service:
    image: node:latest
    volumes:
      - ./app:/app
    working_dir: /app
    command: bash -c "npm install express --save && npm run start"
    ports:
      - 3000:3000
  web:
    build:
      context: .
      dockerfile: ./httpd.dockerfile
    ports:
      - 8080:80